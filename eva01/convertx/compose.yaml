services:
  convertx:
    mem_limit: 64m
    image: ghcr.io/c4illin/convertx:v0.18.0
    container_name: convertx
    restart: unless-stopped
    ports:
      - "3002:3000"
    environment:
      JWT_SECRET: ${JWT_SECRET}
    volumes:
      - /home/lestar/_volumes/convertx/data:/app/data
